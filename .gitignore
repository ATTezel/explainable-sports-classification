# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Trained model artifacts (download from the Kaggle run; too large for git)
*.keras
*.h5
*.weights.h5

# Kaggle API credentials — never commit a token
kaggle.json
.kaggle/

# Kaggle run outputs pulled locally by run_on_kaggle.sh
notebook/kaggle_output/

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out
*.synctex.gz
*.fdb_latexmk
*.fls

# OS
.DS_Store

# Final Teams submission docs — these contain the EDITABLE Overleaf link.
# Keep them out of the public repo so nobody can alter the graded report.
submission/submission_links.docx
submission/submission_overleaf_link.txt
